import { TableOrderByPipe } from './table-order-by.pipe';

describe('TableOrderByPipe', () => {
    it('create an instance', () => {
        const pipe = new TableOrderByPipe();
        expect(pipe).toBeTruthy();
    });
});
