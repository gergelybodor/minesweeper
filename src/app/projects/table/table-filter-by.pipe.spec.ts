import { TableFilterByPipe } from './table-filter-by.pipe';

describe('TableFilterByPipe', () => {
    it('create an instance', () => {
        const pipe = new TableFilterByPipe();
        expect(pipe).toBeTruthy();
    });
});
